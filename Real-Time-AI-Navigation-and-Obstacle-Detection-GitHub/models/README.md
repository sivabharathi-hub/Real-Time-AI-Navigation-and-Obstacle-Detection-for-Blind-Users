# Model files

The supplied project uses `yolov8n.pt` at runtime. The original local project also contained `yolov8s.pt`.

These large binary model files are intentionally not included in this GitHub package. Place the required `yolov8n.pt` file in the repository root before running `obstacle.py`.

Check the model's license/redistribution terms before publishing it.
